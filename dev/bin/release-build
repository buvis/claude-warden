#!/usr/bin/env bash
# Build warden's dist/ (version-stamped from package.json) for the release commit.
# Run by claude-plugins/scripts/release-plugin with CWD = repo root, after bumps.
set -euo pipefail

pnpm run build
